# make sure you install your version of the pytorch suite! This doesn't do it for you
name: generalenv
channels:
  - defaults
dependencies:
  - pip
  - pip:
      - transformers
      - gym
      - stable_baselines3
      - imageio
      - pandas
      - tqdm
      - h5py
      - scikit-learn
      - matplotlib
      - opencv-python
      - line_profiler
      - ipdb
#      - pyaudio
